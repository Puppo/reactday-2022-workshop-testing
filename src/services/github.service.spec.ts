describe("GitHubService", () => {
  describe("searchUsers", () => {
    it.todo("should return the correct result");

    it.todo(
      "should call the api with the query $query and return the expected $expected"
    );

    it.todo("should throw an error if the query is empty");

    it.todo("should throw an error if the serve respond with the code 500");
  });
});

export {};
